# Calvin App — Claude Notes

## Backlog

- **Claude connector copy update** (`frontend/src/components/SettingsView.jsx`):
  Change the instruction text under "Claude connector URL" from the current "Claude Cowork" navigation path to the correct flow:
  > "Tap + to add connector, name your connector, then paste the MCP URL."
  The step-by-step path should read: Claude connector → tap **+** to add connector → name connector → MCP URL.
  (Current text incorrectly references "Claude Cowork → Customize → Connectors".)
